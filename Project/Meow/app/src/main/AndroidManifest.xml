<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pangqianqian.meow">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".cat_circle.cat_circle_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".cat_circle.cat_circle_commend"></activity>
        <activity android:name=".cat_circle.cat_circle_focus"></activity>
        <activity android:name=".cat_circle.cat_circle_nearby"></activity>
        <activity android:name=".cat_circle.cat_circle_new"></activity>
    </application>

</manifest>